name: ethpandaops/devnet-context
version: "1.0"
description: >-
  Use when a workflow needs normalized Ethereum devnet context; resolves network or devnet-group identity, available observability surfaces, likely instance inventory, and an investigation timeframe. Intake stage inside `devnet-debug`; also callable standalone for context resolution alone.
tags: ["devnet", "ethereum", "ethpandaops", "debugging", "context"]
inputs:
  network:
    type: string
    required: true
    description: Target network or devnet name to investigate
  timeframe:
    type: string
    default: past 1 hour
    description: Requested investigation timeframe or epoch range
  include_ci_surfaces:
    type: boolean
    default: true
    description: Whether to include CI-adjacent observability hints such as cluster selection
  notes_url:
    type: string
    description: Optional explicit notes URL override for the devnet
tasks:
  notes:
    uses: ethpandaops/devnet-notes@1
    inputs:
      network: ${{ inputs.network }}
      notes_url: "${{ inputs.?notes_url }}"
    outputs:
      notes_url: notes_url
      notes_summary: notes_summary
      highlights: highlights
      assumptions: assumptions
  collect_context:
    needs: [notes]
    run: collect-devnet-context
    retry:
      max: 2
      on: [error, timeout, schema-validation, quality-gate]
      backoff:
        delay: 5s
        factor: 2
        max-delay: 30s
        jitter: true
      timeout: 1h
    selection:
      capabilities: [reasoning, tool-use]
    quality-gate: "${{ has(result.network_name) && size(result.network_name) > 0 && has(result.investigation_timeframe) && size(result.investigation_timeframe) > 0 && has(result.data_profile) && has(result.summary) && size(result.summary) > 0 }}"
    instructions: |-
      Collect normalized context for a devnet debugging session.

      Use the `panda` CLI and MCP tools as the primary tool surface.

      This task supports two deployment modes:
      - **Remote** (default): devnets accessible via ethpandaops infrastructure (Dora, Loki with ethpandaops instance, ethnode).
      - **Local**: Kurtosis-launched devnets on the local machine with direct service access.

      Detect local mode when the input network looks like a Kurtosis enclave name, the user mentions Kurtosis, or the network cannot be found in remote datasources but `kurtosis enclave ls` shows a matching enclave.

      ## Remote mode

      Panda commands for remote discovery:
      - Network listing: `panda dora networks` to find the network.
      - Datasource check: `panda datasources` to verify available surfaces (Dora, Loki, ClickHouse, Prometheus). Set `has_clickhouse` and `has_prometheus` based on whether the network appears in those datasources.
      - Network overview: `panda dora overview <network>` for epoch, slot, participation, fork status.
      - Loki instance discovery: `panda loki list-datasources` to find the correct Loki datasource name. Do not assume "ethpandaops".
      - Instance discovery: `panda loki label-values <discovered_loki_datasource> instance --query '{network="<network>"}'` to enumerate node instances.
      - Label discovery: `panda loki labels <discovered_loki_datasource> --query '{network="<network>"}'` to find available labels.
      - Node availability: `panda ethnode syncing <network> <instance>` to test reachability.

      Remote instance naming convention: `<cl_type>-<el_type>-<instance_number>` (e.g. `lighthouse-geth-1`). Standard Loki labels: `network` (network name), `instance` (node identity), `ethereum_cl` (CL client type), `ethereum_el` (EL client type), `container_name` (beacon, validator, execution). The `testnet` label also exists as an alias for `network` but prefer `network`. These label names are the standard convention but may differ; if queries return errors or empty results, discover the actual label names dynamically.

      Set `data_profile.mode = "remote"`, `data_profile.loki_schema = "ethpandaops"`, `data_profile.has_direct_node_access = false`.
      Set `data_profile.loki_datasource` to the discovered Loki datasource name when Loki is available, otherwise `""`.
      Set `data_profile.has_clickhouse = true` if `xatu-experimental` datasource exists and the network has data there.
      Set `data_profile.has_prometheus = true` if the `devnets` Prometheus datasource exists.

      ## Local Kurtosis mode

      Discovery steps:
      1. Run `kurtosis enclave inspect <enclave>` to list all services, ports, and status.
      2. Identify CL services (`cl-*`), EL services (`el-*`), and VC services (`vc-*`). Naming convention: `cl-{index}-{cl_type}-{el_type}` and `el-{index}-{el_type}-{cl_type}` (e.g. `cl-1-teku-geth`, `el-1-geth-teku`).
      3. Check for Dora: look for a `dora` service in the enclave. If present, note its HTTP port and set `has_dora = true`.
      4. Check for Loki: test `curl -s http://localhost:3100/ready`. Kurtosis runs Loki on port 3100 when enabled. If it responds, set `has_loki = true`.
      5. Any service not in RUNNING state is itself a finding — include it in the summary.
      6. Local devnets never have ClickHouse or xatu — do not attempt to use them.

      Set `data_profile.mode = "local"`, `data_profile.loki_schema = "kurtosis"`, `data_profile.has_direct_node_access = true`, `data_profile.has_ethnode = false`, `data_profile.has_clickhouse = false`, `data_profile.has_prometheus = false`.
      Set `data_profile.loki_datasource` to the discovered local Loki datasource name when Loki is queryable through `panda`, otherwise `""`.

      ## Common behavior (both modes)

      1. Resolve the requested network. If it cannot be found in any datasource (remote or local), stop and report that the network does not exist.
      2. Determine whether Dora and Loki appear available.
      3. Discover node instances using the mode-appropriate method.
      4. Normalize the active investigation timeframe. Preserve the user-provided timeframe when specific; otherwise default to a recent operational window.
      5. Incorporate any high-signal notes context that affects client readiness, expected flags, or likely topology.
      6. Summarize what evidence surfaces are available and what the next debugging step should rely on.

      Fork-boundary awareness:
      - Check if the network is near or past a recent fork activation (electra, fulu, osaka, etc.).
      - If a fork recently activated, note the fork epoch and whether all client types have confirmed post-fork participation.
      - Fork boundary incidents often manifest as client-specific forks — one client type producing blocks on a separate chain.
      - Record the fork schedule in the context summary when it may be relevant to the incident.
    inputs:
      network: ${{ inputs.network }}
      timeframe: ${{ inputs.timeframe }}
      include_ci_surfaces: ${{ inputs.include_ci_surfaces }}
      notes_url: "${{ has(tasks.notes.outputs.notes_url) ? tasks.notes.outputs.notes_url : '' }}"
      notes_summary: "${{ has(tasks.notes.outputs.notes_summary) ? tasks.notes.outputs.notes_summary : '' }}"
      notes_highlights: "${{ has(tasks.notes.outputs.highlights) ? tasks.notes.outputs.highlights : [] }}"
      notes_assumptions: "${{ has(tasks.notes.outputs.assumptions) ? tasks.notes.outputs.assumptions : [] }}"
    outputs:
      network_name: ${{ result.network_name }}
      network_group: "${{ has(result.network_group) ? result.network_group : '' }}"
      investigation_timeframe: ${{ result.investigation_timeframe }}
      clusters: "${{ has(result.clusters) ? result.clusters : [] }}"
      instances: "${{ has(result.instances) ? result.instances : [] }}"
      data_profile: "${{ result.data_profile }}"
      summary: ${{ result.summary }}
      schema:
        network_name:
          type: string
          description: Resolved concrete network name
        network_group:
          type: string
          description: Devnet group when applicable
        investigation_timeframe:
          type: string
          description: Normalized active investigation timeframe
        clusters:
          type: array
          items:
            type: string
          description: Relevant observability clusters for the network
        instances:
          type: array
          items:
            type: string
          description: Likely CL/EL instance names for the network
        data_profile:
          type: object
          properties:
            mode:
              type: string
              enum: [local, remote]
              description: Deployment mode — local Kurtosis enclave or remote ethpandaops devnet
            has_dora:
              type: boolean
            has_loki:
              type: boolean
            loki_datasource:
              type: string
              description: Resolved Loki datasource name to use for queries; empty when Loki is unavailable or not configured via panda
            has_ethnode:
              type: boolean
            has_clickhouse:
              type: boolean
              description: Whether ClickHouse xatu-experimental datasource has data for this network
            has_prometheus:
              type: boolean
              description: Whether Prometheus devnets datasource is available
            has_direct_node_access:
              type: boolean
              description: Whether CL/EL nodes are directly queryable via localhost ports (true for local Kurtosis)
            loki_schema:
              type: string
              enum: [ethpandaops, kurtosis]
              description: Which Loki label/field conventions to use for log queries
          required: [mode, has_dora, has_loki, loki_datasource, has_ethnode, has_direct_node_access, loki_schema]
          description: Available debugging surfaces and deployment mode
        summary:
          type: string
          description: Concise debugging-context summary
outputs:
  network_name: ${{ tasks.collect_context.outputs.network_name }}
  network_group: ${{ tasks.collect_context.outputs.network_group }}
  investigation_timeframe: ${{ tasks.collect_context.outputs.investigation_timeframe }}
  clusters: ${{ tasks.collect_context.outputs.clusters }}
  instances: ${{ tasks.collect_context.outputs.instances }}
  data_profile: ${{ tasks.collect_context.outputs.data_profile }}
  summary: ${{ tasks.collect_context.outputs.summary }}
  notes_url: ${{ tasks.notes.outputs.notes_url }}
  notes_summary: ${{ tasks.notes.outputs.notes_summary }}
  notes_highlights: ${{ tasks.notes.outputs.highlights }}
  notes_assumptions: ${{ tasks.notes.outputs.assumptions }}
